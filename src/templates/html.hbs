<li>
  <div class="btn-group">
    <a class="btn {{#if options.size}}btn-{{options.size}}{{/if}} btn-default" data-wysihtml5-action="change_view" title="{{locale.html.edit}}" tabindex="-1">
      {{#if options.toolbar.fa}}
        <span class="fa fa-pencil"></span>
      {{else}}
        <span class="glyphicon glyphicon-pencil"></span>
      {{/if}}
    </a>
  </div>
</li>
